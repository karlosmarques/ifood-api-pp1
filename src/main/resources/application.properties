spring.application.name=Ifood
# URL do banco de dados (mude o nome do banco e a porta se necessário)
spring.datasource.url=jdbc:mysql://localhost:3306/ifood?useSSL=false&serverTimezone=UTC

# Usuário e senha do banco
spring.datasource.username=root
spring.datasource.password=karlos24601830

# Dialeto do Hibernate (ajusta SQL para MySQL)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Criação automática de tabelas (opcional, use com cuidado)
spring.jpa.hibernate.ddl-auto=update

# Exibir os comandos SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
